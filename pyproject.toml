[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "k-resdev-skill"
version = "0.1.0b4"
description = "K-ResDev evidence-first Korean R&D administration and research copilot skill pack."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.8",
  "pypdf>=4",
]

[project.optional-dependencies]
dev = [
  "jsonschema>=4",
  "pytest>=8",
]

[project.scripts]
k-resdev = "k_resdev_skill.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
